name: bash
description: Shell-script servers (signal-forwarding exec wrapper applied at spawn)
detect:
  files:
    - serve.sh
    - start.sh
    - run.sh
  port_env_keys:
    - PORT
command: ./serve.sh
