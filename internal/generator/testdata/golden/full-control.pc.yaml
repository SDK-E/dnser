log_level: info
processes:
    auth:
        availability:
            restart: always
        command: bin/serve
        environment:
            - DNSER_DOMAIN=app.acme.io
            - PORT=3000
        log_location: /home/dev/.dnser/logs/auth/auth.log
        readiness_probe:
            exec:
                command: nc -z 127.0.0.1 3000
            period_seconds: 2
        shutdown:
            signal: 15
            timeout_seconds: 15
        working_dir: testdata/fixture-root
version: "0.5"
